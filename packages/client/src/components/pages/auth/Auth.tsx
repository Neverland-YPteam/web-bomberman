import React from 'react';
import { Box } from '@mui/material';

const Auth = () => {
  return (
    <Box
    sx={{
      height: '100vh',
      display: 'flex',
    }}
    >
        <p>Authorization page</p>
    </Box>
  );
};

export default Auth;
